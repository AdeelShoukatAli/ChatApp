<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/new_msg_menu"
          android:title="@string/new_message"
          app:showAsAction="ifRoom"/>

    <item android:id="@+id/changeInfoMenu"
          android:title="@string/change_info"
          app:showAsAction="never"/>

    <item android:id="@+id/reset_password"
          android:title="@string/reset_password"
          app:showAsAction="never"/>

    <item android:id="@+id/log_out_menu"
          android:title="@string/log_out"
          app:showAsAction="never"/>

</menu>